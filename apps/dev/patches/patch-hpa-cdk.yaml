apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: fastapi-hpa
  namespace: fastapi
spec:
  maxReplicas: 50
  minReplicas: 2
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 30
